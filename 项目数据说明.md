# MyJD 项目数据说明文档

## 数据概览

本项目采用 **JSON文件** 作为数据源，通过 `DataRepository` 单例模式统一管理所有数据的读取、缓存和持久化。

---

## 一、数据文件清单

### 数据存储位置
- **只读数据（初始数据）：** `app/src/main/assets/data/` - 24个JSON文件
- **可写数据（运行时）：** `context.filesDir/persistent_data/` - 运行时持久化文件

### 1.1 商品相关数据（9个文件）

#### 1. `products.json` - 商品列表
**数据结构：** `List<Product>`  
**用途：** 首页商品展示  
**调用位置：**
- `DataRepository.loadProducts()` → `HomeViewModel`
- `HomeScreen.kt` - 首页商品列表

**主要字段：**
```json
{
  "id": "商品ID",
  "name": "商品名称",
  "price": 当前价格,
  "originalPrice": 原价,
  "brand": "品牌",
  "category": "分类",
  "imageUrl": "封面图片路径",
  "storeId": "店铺ID",
  "storeName": "店铺名称",
  "colors": ["颜色选项"],
  "versions": ["版本选项"],
  "stock": 库存数量,
  "isInStock": 是否有货,
  "rating": 评分,
  "reviewCount": 评论数,
  "description": "商品描述",
  "specifications": { "规格详情" }
}
```

**包含商品类型：**
- 手机：iPhone 15、华为P60、华为Nova11、华为Mate60
- 电脑：ThinkPad E14
- 超市商品：五常大米、特仑苏牛奶、金龙鱼食用油等
- 其他：耐克鞋、索尼耳机等（共30+个商品）

---

#### 2. `product_detail.json` - 通用商品详情
**数据结构：** `ProductDetail`  
**用途：** iPhone 15等通用商品的详细信息  
**调用位置：**
- `DataRepository.loadProductDetail()` → `ProductDetailViewModel`
- `ProductDetailScreen.kt` - 商品详情页

**主要字段：**
```json
{
  "id": "商品ID",
  "title": "完整标题",
  "images": ["商品图片数组"],
  "currentPrice": 当前价格,
  "originalPrice": 原价,
  "subsidyPrice": "补贴说明",
  "soldCount": "销量",
  "storeName": "店铺名称",
  "colors": [颜色选项数组],
  "selectedColorIndex": 默认颜色索引,
  "purchaseTypes": ["购买方式"],
  "specifications": {规格信息},
  "tags": ["标签"],
  "isFavorite": 是否收藏,
  "deliveryInfo": {配送信息},
  "tradeIn": {以旧换新信息},
  "stores": [线下门店],
  "reviews": {评价信息}
}
```

---

#### 3-6. 特定商品详情文件
**华为Mate60系列：**
- `huawei_mate60_detail.json` - 详情数据
- `huawei_mate60_specs.json` - 规格选择数据

**华为Nova11系列：**
- `huawei_nova11_detail.json` - 详情数据
- `huawei_nova11_specs.json` - 规格选择数据

**华为P60系列：**
- `huawei_p60_detail.json` - 详情数据
- `huawei_p60_specs.json` - 规格选择数据

**调用位置：**
- `DataRepository.getHuaweiMate60ProductDetail()` → `HuaweiMate60DetailViewModel`
- `DataRepository.loadHuaweiMate60ProductSpec()` → `ProductSpecViewModel`
- 对应的 `xxxDetailScreen.kt` 页面

---

#### 7. `thinkpad_detail.json` - ThinkPad商品详情
**数据结构：** `ProductDetail`  
**用途：** ThinkPad笔记本详情页  
**调用位置：**
- `DataRepository.getThinkPadProductDetail()` → `ThinkPadDetailViewModel`
- `ThinkPadDetailScreen.kt`

---

#### 8. `product_specs.json` - 通用规格选择
**数据结构：** `ProductSpec`  
**用途：** 商品规格选择弹窗（系列、颜色、存储容量）  
**调用位置：**
- `DataRepository.loadProductSpec()` → `ProductSpecViewModel`
- `ProductSpecDialog.kt` - 规格选择弹窗

**主要字段：**
```json
{
  "productId": "商品ID",
  "defaultSeries": "默认系列",
  "defaultColor": "默认颜色",
  "defaultStorage": "默认容量",
  "series": [系列选项],
  "colors": [
    {
      "name": "颜色名",
      "colorCode": "颜色代码",
      "image": "图片",
      "price": 价格增量,
      "available": 是否可选,
      "stockTag": "库存标签"
    }
  ],
  "storage": [容量选项],
  "promotionInfo": {促销信息}
}
```

---

#### 9. `search_results.json` - 搜索结果
**数据结构：** `List<Product>`  
**用途：** 搜索结果页展示  
**调用位置：**
- `DataRepository.getSearchResults()` → `SearchResultViewModel`
- `SearchResultScreen.kt` - 搜索结果页

**特点：**
- 包含完整的Product信息
- 额外包含 `sales` 字段（销量数据）
- 主要为iPhone系列商品（15个搜索结果）

---

### 1.2 订单与购物车（3个文件）

#### 10. `cart_items.json` - 购物车数据
**数据结构：** `List<CartItem>` 或 `List<CartItemSpec>`  
**用途：** 购物车商品列表  
**调用位置：**
- `DataRepository.loadInitialCartDataFromAssets()` → 初始加载
- `DataRepository.getSpecShoppingCart()` → `CartScreen`
- **运行时持久化：** `context.filesDir/persistent_data/cart_items.json`

**主要字段：**
```json
{
  "id": "购物车项ID",
  "product": {完整商品信息},
  "quantity": 数量,
  "isSelected": 是否选中,
  "addedTime": 添加时间戳
}
```

**数据操作方法：**
- `addSpecItemToCart()` - 添加商品
- `removeSpecItemFromCart()` - 移除商品
- `updateSpecItemQuantity()` - 更新数量
- `toggleSpecItemSelection()` - 切换选中状态
- `clearCart()` - 清空购物车

---

#### 11. `orders.json` - 订单数据
**数据结构：** `List<Order>`  
**用途：** 订单列表展示  
**调用位置：**
- `DataRepository.loadInitialOrdersFromAssets()` → 初始加载
- `DataRepository.getOrders()` → `OrderViewModel`
- `OrderScreen.kt` - 订单页面
- **运行时持久化：** `context.filesDir/persistent_data/orders.json`

**主要字段：**
```json
{
  "id": "订单ID",
  "userId": "用户ID",
  "items": [订单商品列表],
  "status": "订单状态（枚举）",
  "paymentMethod": "支付方式（枚举）",
  "shippingAddress": {收货地址},
  "totalAmount": 总金额,
  "createTime": 创建时间,
  "payTime": 支付时间,
  "shipTime": 发货时间,
  "receiveTime": 收货时间,
  "cancelTime": 取消时间,
  "cancelReason": "取消原因（枚举）",
  "trackingNumber": "物流单号",
  "isReviewed": 是否已评价,
  "usedCouponId": "使用的优惠券ID",
  "usedCouponDescription": "优惠券描述"
}
```

**订单状态枚举：**
- `PENDING_PAYMENT` - 待付款
- `PENDING_RECEIPT` - 待收货
- `PENDING_SHIPMENT` - 待使用
- `PENDING_REVIEW` - 待评价
- `COMPLETED` - 已完成
- `CANCELLED` - 已取消

**数据操作方法：**
- `createOrderFromSpecCart()` - 从购物车创建订单
- `payOrder()` - 支付订单
- `cancelOrder()` - 取消订单
- `deleteOrder()` - 删除订单
- `getOrderById()` - 获取订单详情

---

#### 12. `refund_records.json` - 退款记录
**数据结构：** `List<RefundRecord>`  
**用途：** 售后服务、退款记录展示  
**调用位置：** 预留接口（暂未在UI中完全实现）

**主要字段：**
```json
{
  "id": "退款记录ID",
  "orderId": "关联订单ID",
  "orderItemId": "订单商品ID",
  "serviceType": "服务类型（仅退款/退货退款/换货）",
  "status": "退款状态",
  "reason": "退款原因",
  "description": "详细描述",
  "refundAmount": 退款金额,
  "applyTime": 申请时间,
  "processTime": 处理时间,
  "completeTime": 完成时间,
  "images": ["凭证图片"],
  "adminReply": "客服回复"
}
```

---

### 1.3 地址管理（1个文件）

#### 13. `addresses.json` - 收货地址
**数据结构：** `List<Address>`  
**用途：** 收货地址管理  
**调用位置：**
- `DataRepository.loadAddresses()` → `AddressListViewModel`
- `AddressListScreen.kt` - 地址列表页
- `AddressDetailScreen.kt` - 地址编辑页
- **运行时持久化：** `context.filesDir/persistent_data/addresses.json`

**主要字段：**
```json
{
  "id": "地址ID",
  "recipientName": "收件人",
  "phoneNumber": "电话",
  "province": "省",
  "city": "市",
  "district": "区",
  "detailAddress": "详细地址",
  "postCode": "邮编",
  "isDefault": 是否默认,
  "tag": "标签（家/公司/学校等）",
  "createTime": 创建时间
}
```

**数据操作方法：**
- `addAddress()` - 添加地址
- `updateAddress()` - 更新地址
- `deleteAddress()` - 删除地址
- `setDefaultAddress()` - 设置默认地址
- `getDefaultAddress()` - 获取默认地址

---

### 1.4 消息与聊天（2个文件）

#### 14. `messages.json` - 消息数据
**数据结构：** 复杂结构（包含多种消息类型）  
**用途：** 聊天页面的消息列表  
**调用位置：**
- `DataRepository.loadMessages()` → `ChatViewModel`
- `DataRepository.loadConversationData()` → 会话数据
- `ChatScreen.kt` - 聊天页面
- **运行时持久化：** `context.filesDir/persistent_data/new_messages.json`

**主要字段：**
```json
{
  "legacy_messages": [旧版消息格式],
  "conversations": [
    {
      "id": "会话ID",
      "senderName": "发送者",
      "senderAvatar": "头像",
      "type": "消息类型",
      "subType": "子类型",
      "isMuted": 是否静音,
      "hasUnreadDot": 是否有未读红点,
      "messages": [
        {
          "id": "消息ID",
          "senderId": "发送者ID",
          "senderType": "发送者类型（USER/SYSTEM）",
          "content": "消息内容",
          "type": "消息类型（TEXT/IMAGE/PRODUCT等）",
          "timestamp": 时间戳,
          "isRead": 是否已读
        }
      ]
    }
  ]
}
```

**消息类型：**
- `CUSTOMER_SERVICE` - 客服消息
- `LOGISTICS` - 物流消息
- `REVIEW` - 评价消息
- `AFTER_SALES` - 售后消息

**数据操作方法：**
- `getConversationSummaries()` - 获取会话摘要列表
- `sendMessage()` - 发送消息
- `deleteConversation()` - 删除会话
- `toggleMute()` - 切换静音

---

#### 15. `notifications.json` - 系统通知
**数据结构：** `List<SystemNotification>`  
**用途：** 系统通知、订单通知、促销通知  
**调用位置：** 预留接口（可用于通知中心）

**主要字段：**
```json
{
  "id": "通知ID",
  "type": "通知类型（SYSTEM/ORDER/PROMOTION/DELIVERY）",
  "title": "标题",
  "content": "内容",
  "imageUrl": "图片URL",
  "isRead": 是否已读,
  "timestamp": 时间戳,
  "relatedOrderId": "关联订单ID",
  "relatedProductId": "关联商品ID",
  "actionUrl": "操作URL"
}
```

---

### 1.5 用户与会员（2个文件）

#### 16. `user_profile.json` - 用户资料
**数据结构：** `JsonObject`（灵活结构）  
**用途：** 用户个人信息、会员信息  
**调用位置：**
- `DataRepository.loadUserProfile()` → `MeViewModel`
- 间接用于 `MeScreen.kt` - 个人中心

**主要字段：**
```json
{
  "id": "用户ID",
  "username": "用户名",
  "nickname": "昵称",
  "avatar": "头像URL",
  "phone": "手机号",
  "email": "邮箱",
  "isLoggedIn": 是否登录,
  "memberLevel": "会员等级",
  "points": 积分,
  "balance": 余额,
  "coupons": [优惠券列表],
  "preferences": {偏好设置},
  "statistics": {
    "totalOrders": 总订单数,
    "totalSpent": 总消费,
    "favoriteProductsCount": 收藏商品数,
    "reviewsCount": 评价数
  }
}
```

---

#### 17. `me_tab.json` - 个人中心页面数据
**数据结构：** `MeTabData`  
**用途：** "我的"页面的功能模块配置  
**调用位置：**
- `DataRepository.loadMeTabData()` → `MeViewModel`
- `MeScreen.kt` - 个人中心页面

**主要字段：**
```json
{
  "memberBenefits": [
    {
      "id": "功能ID",
      "name": "功能名称",
      "value": "显示值",
      "description": "描述",
      "iconEmoji": "图标emoji"
    }
  ],
  "promoBanners": [促销横幅],
  "quickActions": [快捷功能],
  "serviceGrid": [服务网格]
}
```

**包含功能：**
- 优惠券、京豆、红包、白条
- 订单管理、收藏、浏览历史
- 客服、设置等

---

### 1.6 店铺相关（2个文件）

#### 18. `shop_data.json` - 店铺主页数据
**数据结构：** `ShopPageData`  
**用途：** 店铺主页展示  
**调用位置：**
- `DataRepository.loadShopPageData()` → `ShopPagePresenter`
- `DataRepository.loadHuaweiShopPageData()` → `HuaweiShopPresenter`
- `ShopPageScreen.kt` / `HuaweiShopScreen.kt`

**主要字段：**
```json
{
  "shopInfo": {
    "id": "店铺ID",
    "name": "店铺名称",
    "avatar": "店铺头像",
    "followers": "粉丝数",
    "isFollowed": 是否已关注,
    "serviceBanner": "服务横幅",
    "description": "店铺描述"
  },
  "statistics": [
    {
      "label": "统计标签",
      "value": "统计值",
      "icon": "图标"
    }
  ],
  "categories": [分类标签],
  "products": [店铺商品列表]
}
```

---

#### 19. `stores.json` - 店铺列表
**数据结构：** `List<Store>`  
**用途：** 店铺信息、线下门店信息  
**调用位置：** 预留接口（可用于店铺列表页）

**主要字段：**
```json
{
  "id": "店铺ID",
  "name": "店铺名称",
  "logo": "LOGO",
  "description": "简介",
  "rating": 评分,
  "followerCount": 粉丝数,
  "isFollowed": 是否关注,
  "products": [],
  "establishedTime": 创立时间,
  "location": "地址",
  "serviceScore": 服务评分,
  "logisticsScore": 物流评分,
  "qualityScore": 质量评分
}
```

---

### 1.7 营销相关（1个文件）

#### 20. `banners.json` - 轮播图
**数据结构：** `List<Banner>`  
**用途：** 首页轮播图展示  
**调用位置：**
- `DataRepository.loadBanners()` → `HomeViewModel`
- `BannerSection.kt` - 轮播图组件

**主要字段：**
```json
{
  "id": "横幅ID",
  "title": "标题",
  "subtitle": "副标题",
  "imageUrl": "图片URL",
  "actionUrl": "跳转URL",
  "backgroundColor": "背景色",
  "textColor": "文字颜色",
  "isActive": 是否激活,
  "sortOrder": 排序,
  "startTime": 开始时间,
  "endTime": 结束时间,
  "type": "类型（PROMOTION/PRODUCT/CATEGORY）"
}
```

---

### 1.8 任务日志（1个文件）

#### 21. `task_logs.json` - 任务日志
**数据结构：** `List<TaskLog>`  
**用途：** 记录用户操作任务（用于测试和统计）  
**调用位置：** 各个TaskLogger工具类

**主要字段：**
```json
{
  "id": "日志ID",
  "taskType": "任务类型",
  "taskDescription": "任务描述",
  "action": "操作",
  "details": {详细信息},
  "timestamp": 时间戳,
  "completed": 是否完成
}
```

**任务类型：**
- `SEARCH` - 搜索操作
- `ADD_TO_CART` - 加入购物车
- `VIEW_PRODUCT` - 查看商品
- `PLACE_ORDER` - 下单
- 其他...（共18种任务类型）

---

## 二、数据调用流程

### 2.1 数据加载流程

```
┌──────────────────────────────────────────────────┐
│                   App启动                         │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────┐
│        DataRepository.getInstance()               │
│        创建单例，初始化数据目录                    │
└────────────────┬─────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────┐
│        loadPersistentData()                       │
│        从运行时文件加载持久化数据                  │
└────────────────┬─────────────────────────────────┘
                 │
                 ├─→ 如果存在持久化文件 → 加载运行时数据
                 │
                 └─→ 如果不存在 → loadFromAssets()
                                    │
                                    ▼
                      ┌────────────────────────────┐
                      │  从 assets/data/ 加载初始  │
                      │  JSON文件作为默认数据      │
                      └────────────────────────────┘
```

### 2.2 典型调用链示例

#### 示例1：首页商品列表
```
HomeScreen (Composable)
    ↓
HomeViewModel.refreshData()
    ↓
DataRepository.loadProducts()
    ↓
读取 assets/data/products.json
    ↓
解析为 List<Product>
    ↓
更新 ViewModel StateFlow
    ↓
UI自动刷新显示商品列表
```

#### 示例2：商品详情页
```
用户点击商品（productId: "iphone15_001"）
    ↓
导航到 ProductDetailScreen(productId)
    ↓
ProductDetailViewModel.loadProductDetail(productId)
    ↓
DataRepository.loadProductDetail(productId)
    ↓
读取 assets/data/product_detail.json
    ↓
根据productId匹配或默认返回
    ↓
解析为 ProductDetail对象
    ↓
更新 ViewModel StateFlow
    ↓
UI显示商品详情
```

#### 示例3：购物车操作
```
用户选择规格并点击"加入购物车"
    ↓
ProductSpecViewModel.addToCart()
    ↓
DataRepository.addSpecItemToCart(CartItemSpec)
    ↓
1. 添加到内存列表 specShoppingCart
2. 保存到 persistent_data/cart_items.json
3. 触发 TaskLogger 记录
    ↓
返回成功
    ↓
显示Toast提示"已加入购物车"
```

#### 示例4：订单创建
```
用户在结算页点击"立即购买"
    ↓
SettleViewModel.createOrder()
    ↓
DataRepository.createOrderFromSpecCart()
    ↓
1. 从购物车获取选中商品
2. 创建Order对象
3. 添加到 runtimeOrders 列表
4. 保存到 persistent_data/orders.json
5. 从购物车移除已购买商品
6. 更新购物车文件
    ↓
返回订单ID
    ↓
导航到支付成功页面
```

---

## 三、数据持久化策略

### 3.1 持久化的数据类型

| 数据类型 | 初始数据源 | 运行时存储 | 是否持久化 |
|---------|-----------|-----------|----------|
| 商品列表 | products.json | 内存 | ❌ 只读 |
| 商品详情 | *_detail.json | 内存 | ❌ 只读 |
| 商品规格 | *_specs.json | 内存 | ❌ 只读 |
| **购物车** | cart_items.json | persistent_data/ | ✅ 持久化 |
| **订单** | orders.json | persistent_data/ | ✅ 持久化 |
| **地址** | addresses.json | persistent_data/ | ✅ 持久化 |
| **消息** | messages.json | persistent_data/ | ✅ 持久化 |
| 静音设置 | - | persistent_data/ | ✅ 持久化 |
| 用户资料 | user_profile.json | 内存 | ❌ 只读 |
| 店铺数据 | shop_data.json | 内存 | ❌ 只读 |
| 轮播图 | banners.json | 内存 | ❌ 只读 |

### 3.2 持久化机制

**保存时机：**
- 购物车：每次增删改操作后立即保存
- 订单：创建、支付、取消、删除后保存
- 地址：新增、修改、删除后保存
- 消息：发送消息、删除会话后保存

**加载优先级：**
1. 首先尝试从运行时持久化文件加载
2. 如果文件不存在或为空，从assets加载初始数据
3. 如果assets也加载失败，返回空列表

---

## 四、数据访问接口总览

### 4.1 商品相关接口（9个）

| 方法名 | 返回类型 | 用途 |
|-------|---------|------|
| `loadProducts()` | `List<Product>` | 获取商品列表 |
| `getSearchResults()` | `List<Product>` | 获取搜索结果 |
| `loadProductDetail(productId)` | `ProductDetail` | 加载商品详情 |
| `getHuaweiP60ProductDetail()` | `ProductDetail?` | 华为P60详情 |
| `getHuaweiMate60ProductDetail()` | `ProductDetail?` | 华为Mate60详情 |
| `getHuaweiNova11ProductDetail()` | `ProductDetail?` | 华为Nova11详情 |
| `getThinkPadProductDetail()` | `ProductDetail?` | ThinkPad详情 |
| `loadProductSpec(productId)` | `ProductSpec` | 加载商品规格 |
| `loadHuaweiXXXProductSpec()` | `ProductSpec` | 特定商品规格 |

### 4.2 购物车接口（10个）

| 方法名 | 返回类型 | 用途 |
|-------|---------|------|
| `getSpecShoppingCart()` | `List<CartItemSpec>` | 获取购物车列表 |
| `addSpecItemToCart()` | `Boolean` | 添加商品到购物车 |
| `removeSpecItemFromCart()` | - | 从购物车移除 |
| `updateSpecItemQuantity()` | - | 更新商品数量 |
| `toggleSpecItemSelection()` | - | 切换选中状态 |
| `clearCart()` | - | 清空购物车 |
| `getSpecCartTotalCount()` | `Int` | 获取商品总数 |
| `getSelectedSpecCartTotalPrice()` | `Double` | 获取选中商品总价 |
| `getSelectedSpecCartCount()` | `Int` | 获取选中商品数量 |
| `getSelectedCartItems()` | `List<CartItemSpec>` | 获取选中商品列表 |

### 4.3 订单接口（8个）

| 方法名 | 返回类型 | 用途 |
|-------|---------|------|
| `getOrders()` | `List<Order>` | 获取所有订单 |
| `createOrderFromSpecCart()` | `String?` | 从购物车创建订单 |
| `payOrder(orderId)` | `Boolean` | 支付订单 |
| `cancelOrder(orderId, reason)` | `Boolean` | 取消订单 |
| `deleteOrder(orderId)` | `Boolean` | 删除订单 |
| `getOrderById(orderId)` | `Order?` | 根据ID获取订单 |
| `getLatestPendingOrderId()` | `String?` | 获取最新待付款订单 |
| `confirmReceipt(orderId)` | `Boolean` | 确认收货 |

### 4.4 地址接口（7个）

| 方法名 | 返回类型 | 用途 |
|-------|---------|------|
| `loadAddresses()` | `List<Address>` | 加载地址列表 |
| `getAddressById(id)` | `Address?` | 根据ID获取地址 |
| `getDefaultAddress()` | `Address?` | 获取默认地址 |
| `addAddress(address)` | `Boolean` | 添加地址 |
| `updateAddress(address)` | `Boolean` | 更新地址 |
| `deleteAddress(id)` | `Boolean` | 删除地址 |
| `setDefaultAddress(id)` | `Boolean` | 设置默认地址 |

### 4.5 消息与会话接口（9个）

| 方法名 | 返回类型 | 用途 |
|-------|---------|------|
| `loadMessages()` | `List<Message>` | 加载消息列表 |
| `loadConversationData()` | `ConversationData` | 加载会话数据 |
| `getConversationSummaries()` | `List<ConversationSummary>` | 获取会话摘要 |
| `sendMessage()` | `Boolean` | 发送消息 |
| `deleteConversation(id)` | `Boolean` | 删除会话 |
| `toggleMute(sender)` | - | 切换静音 |
| `getNewMessages()` | `List<ChatMessage>` | 获取新消息 |
| `getMuteSetting(sender)` | `Boolean` | 获取静音设置 |
| `getAllMuteSettings()` | `List<MuteSetting>` | 获取所有静音设置 |

### 4.6 其他接口

| 方法名 | 返回类型 | 用途 |
|-------|---------|------|
| `loadBanners()` | `List<Banner>` | 加载轮播图 |
| `loadMeTabData()` | `MeTabData` | 加载个人中心数据 |
| `loadUserProfile()` | `JsonObject` | 加载用户资料 |
| `loadShopPageData()` | `ShopPageData` | 加载店铺数据 |
| `getAvailableCoupons()` | `List<Coupon>` | 获取可用优惠券 |

---

## 五、数据统计与日志

### 5.1 任务日志系统

项目包含18个TaskLogger，用于记录特定操作：

| Logger | 记录内容 | 调用位置 |
|--------|---------|---------|
| TaskOneLogger | 首次打开App | MainActivity |
| TaskFourLogger | 搜索并查看商品详情 | SearchResultScreen |
| TaskSixLogger | 点击首页分类 | HomeScreen |
| TaskEightLogger | 购物车商品数量 | CartScreen |
| TaskNineLogger | 选中购物车总价 | CartScreen |
| TaskTenLogger | 待收货订单数 | OrderScreen |
| TaskElevenLogger | 消息数量 | ChatScreen |
| TaskFourteenLogger | 商品评价数 | ProductDetailScreen |
| TaskSixteenLogger | 收货地址数量 | AddressListScreen |
| TaskSeventeenLogger | 店铺页面加载 | ShopPageScreen |
| TaskEighteenLogger | 订单取消/删除 | OrderScreen |

### 5.2 日志文件位置

**运行时日志：**
- `context.filesDir/task_one_log.txt`
- `context.filesDir/task_four_logs.json`
- `context.filesDir/task_six_log.txt`
- 等...

**日志访问方法：**
```kotlin
// 示例
val taskOneLog = DataRepository.getTaskOneLog()
val taskOneCompleted = DataRepository.getTaskOneCompletionDetails()
```

---

## 六、数据模型映射关系

### 6.1 JSON → Kotlin 映射

```
assets/data/*.json  →  Data Models  →  Domain Models  →  Presentation Models
                       (data/model/)   (domain/model/)   (presentation/model/)
```

**示例：商品数据流**
```
products.json (JSON文件)
    ↓ 解析
Product (domain/model/Product.kt) - 业务模型
    ↓ 转换
ProductDetail (data/model/ProductDetail.kt) - 详情数据模型
    ↓ 使用
ProductDetailScreen - UI展示
```

### 6.2 核心数据类型关系图

```
Product (商品基本信息)
    ├─ ProductDetail (商品详情)
    │   ├─ ProductColor (颜色选项)
    │   ├─ DeliveryInfo (配送信息)
    │   ├─ TradeInInfo (以旧换新)
    │   ├─ StoreInfo (门店信息)
    │   └─ ReviewInfo (评价信息)
    │
    └─ ProductSpec (规格选择)
        ├─ SeriesOption (系列)
        ├─ ColorOption (颜色)
        ├─ StorageOption (容量)
        └─ PromotionInfo (促销)

CartItemSpec (购物车商品)
    └─ 包含完整Product信息 + 选择的规格

Order (订单)
    ├─ OrderItem[] (订单商品)
    │   └─ Product (商品信息)
    ├─ Address (收货地址)
    └─ OrderStatus (订单状态)
```

---

## 七、总结

### 数据文件统计
- **总文件数：** 24个JSON文件
- **总数据量：** 约100KB+
- **数据类型：** 9大类（商品、订单、地址、消息等）
- **持久化数据：** 5类（购物车、订单、地址、消息、设置）

### 调用层次
```
UI Layer (Screen/Component)
    ↓
ViewModel Layer
    ↓
DataRepository (单例)
    ↓
├─ Memory Cache (运行时数据)
├─ Persistent Storage (运行时文件)
└─ Assets (只读初始数据)
```

### 核心特点
✅ **单一数据源**：DataRepository统一管理  
✅ **数据持久化**：关键数据自动保存  
✅ **类型安全**：强类型Kotlin数据类  
✅ **异步处理**：Coroutines + Flow  
✅ **错误处理**：完善的异常捕获  
✅ **任务追踪**：18个TaskLogger记录操作  

---
**适用项目：** MyJD 京东模拟App
